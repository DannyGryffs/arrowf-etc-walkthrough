function mulitply(a, b){
return a * b 
}
// -----------------------------
const mulitply3 = (a, b) => {
    return a * b
}
// -----------------------------
const muliply2 = (a, b) => a * b

//implicit return      ^^^^^^^^^
//BC function is only one line
//------------------------------

// why do we use arrowFunctions??
//because of simplicity-shortcuts are nice lol

//write an arrowFunction that takes in one parameter, and returns that parameter's square root.
//you can assume the arguments you are given will be a number

const sqrt = (num) => Math.sqrt(num)

sqrt(25) 
